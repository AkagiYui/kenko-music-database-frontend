{
  "name": "kenko-music-database-frontend",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "dev": "turbo run dev",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "build": "turbo run build",
    "clean": "turbo run clean",
    "purge": "turbo run purge",
    "preview": "pnpm --filter main preview"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "packageManager": "pnpm@10.8.0",
  "devDependencies": {
    "rimraf": "^6.0.1",
    "turbo": "^2.5.0",
    "vite": "catalog:cli",
    "vitest": "catalog:cli"
  }
}
